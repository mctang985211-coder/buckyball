# LayerNormBall has no implementation-specific parameters.
# eps = 1e-12, gamma/beta bank layout, and the fail-hard table are fixed in
# the instruction contract (see emu/src/69_layernorm.rs).
[ball]
